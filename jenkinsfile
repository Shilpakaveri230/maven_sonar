     pipeline {
        agent none
        stages {
         
          stage("build & Sonarqube Analysis") {
            agent any
            steps {
              withSonarQubeEnv('sona_server') {
                sh 'mvn clean package sonar:sonar'
              }
            }
          }
        }
      }
